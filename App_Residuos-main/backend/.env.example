# Servidor
PORT=8080
NODE_ENV=development

# Supabase
# URL de tu proyecto (Settings -> API)
SUPABASE_URL=https://ybbhmauqilygldknzpcv.supabase.co
# Service Role Key (Settings -> API -> service_role). NUNCA la expongas en el cliente/Flutter.
SUPABASE_SERVICE_ROLE_KEY=tu_service_role_key_aqui

# JWT
JWT_ACCESS_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio
JWT_REFRESH_SECRET=cambia_esto_por_otro_secreto_largo_y_aleatorio_distinto
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS - dominios permitidos separados por coma (usa * solo en desarrollo)
CORS_ORIGINS=*

# Servidor
PORT=8080
NODE_ENV=development

# Supabase (service role key — solo backend, nunca en cliente)
SUPABASE_URL=https://ybbhmauqilygldknzpcv.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# JWT (debe coincidir con lib/services/crypto_service.dart en Flutter)
JWT_SECRET=your-super-secret-key-change-in-production
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS (separar múltiples orígenes con coma)
CORS_ORIGIN=*
